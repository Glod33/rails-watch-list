<!DOCTYPE html>
<html>
  <head>
    <title>RailsWatchList</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <nav class='navbar-rutger'>
      <i class="fa-solid fa-film fa-3x navbar-brand navbar-logo"></i>
      <%= link_to 'my lists', lists_path, class: "btn btn-outline-primary" %>
      <%= link_to 'create a new list', new_list_path, class: "btn btn-outline-primary" %>
    </nav>
    <%= yield %>
  </body>
</html>
